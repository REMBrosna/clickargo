import { StyleSheet } from 'react-native';
import { horizontalScale, verticalScale, moderateScale } from "../constants/utils";

export const ckComponentStyles = StyleSheet.create({

    ckbuttons: {
        height: 48,
        width: "100%",
        borderRadius: 10,
        justifyContent: "space-evenly",
        alignItems: "center"
    }

})