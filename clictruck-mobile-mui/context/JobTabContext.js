import { createContext } from "react";

/** This for the tabId update change everytime there is a confirmation to start a job from New or Paused listing to move to Active screen. */
const JobTabContext = createContext();

export default JobTabContext;
